# Contributing

This repository is intentionally small. Keep changes focused on the local exam
materials workflow.

Before opening a pull request:

```bash
pip install -r requirements.txt
pytest -q
```

Do not add real scans, private notes, generated study materials, credentials,
external API integrations, RAG, vector databases, embeddings, web UI, or
autonomous agents.
